недочёты/предположения:
    ExchangeRateService начинает походить на GOD class (нарушается SRP)
    отсутствие история git
    id можно было бы генерировать с помощью UUID


архитектура (выполнено таким образом, т.к. в ТЗ было указано на рассмотрение разработчика):
   * в ExchangeRateController параметр LocalDate date можно засунуть в DTO в случае добавления новых параметров запроса
   * т.к в тз было указано использование бд, то при запросе на эндпоинт запрос отправляется сразу в бд и в отсутсвии данных
     отправляется запрос на API. Можно было банально сохранять ответ в одну таблицу, в которой данных не так много:
     "Cur_ID": 394,
     "Date": "2024-06-30T00:00:00",
     "Cur_Abbreviation": "KWD",
     "Cur_Scale": 1,
     "Cur_Name": "Кувейтский динар",
     "Cur_OfficialRate": 10.3111
     и повесить индекс на аббревиатуру(код) валюты (в api 31 валюта, так что надо будет посмотреть на производительность/затраты по памяти) и на дату,
     но тогда это бы нарушало нормальные формы. Т.к. Нацбанк устанавливает курс на день, то 365 (366) записей в год не так и много и на скорость бы не сильно влияло.
   * решение с множествами придуман для уменьшения нагрузки на бд
Если возникнут вопросы по другим частям приложения смело задавайте вопросы
@onkonfeton
